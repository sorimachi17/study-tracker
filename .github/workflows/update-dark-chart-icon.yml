name: Update dark chart app icon
on:
  push:
    paths:
      - '.github/workflows/update-dark-chart-icon.yml'
permissions:
  contents: write
jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - name: Update icon source and references
        run: python3 .github/scripts/update-dark-chart-icon.py
      - name: Generate PNG app icons
        run: |
          if command -v convert >/dev/null 2>&1; then
            convert -background none app-icon.svg -resize 180x180 app-icon-180.png
            convert -background none app-icon.svg -resize 192x192 app-icon-192.png
            convert -background none app-icon.svg -resize 512x512 app-icon-512.png
          elif command -v rsvg-convert >/dev/null 2>&1; then
            rsvg-convert -w 180 -h 180 app-icon.svg -o app-icon-180.png
            rsvg-convert -w 192 -h 192 app-icon.svg -o app-icon-192.png
            rsvg-convert -w 512 -h 512 app-icon.svg -o app-icon-512.png
          else
            python3 -m pip install --quiet cairosvg
            python3 - <<'PY'
          import cairosvg
          for size in (180,192,512):
              cairosvg.svg2png(url='app-icon.svg', write_to=f'app-icon-{size}.png', output_width=size, output_height=size)
          PY
          fi
      - name: Commit app icon
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/update-dark-chart-icon.yml .github/scripts/update-dark-chart-icon.py
          git add app-icon.svg app-icon-180.png app-icon-192.png app-icon-512.png index.html manifest.webmanifest sw.js
          git commit -m "Update app icon to dark progress chart"
          git push origin main
